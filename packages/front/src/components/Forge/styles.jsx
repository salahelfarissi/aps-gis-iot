const width = '100% !important';
const height = '100% !important';
const viewerWidth = '500px !important';
const viewerHeight = '500px !important';
const position = 'fixed !important';

const appStyles = {
  width: width,
  height: height,
  position: position,
}

const viewerStyles = {
  width: viewerWidth,
  height: viewerHeight,
}

export const styles = {
  appStyles: appStyles,
  viewerStyles: viewerStyles,
}